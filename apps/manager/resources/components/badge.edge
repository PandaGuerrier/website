@if(href)
  <a
    href="{{ href }}"
    class="bg-primary-100 text-primary-500 px-3 py-0.5 rounded-full"
  >
    {{ value }}
  </a>
@else
  <div class="bg-primary-100 text-primary-700 px-3 py-0.5 rounded-full">
    {{ value }}
  </div>
@end
